Place your template PNG images here.
Each file should be a cropped screenshot of a UI element.

Required templates (named exactly as below):
  state_village.png        — A distinctive element of the village HUD
  state_attack_menu.png    — The attack menu / sword button panel
  state_searching.png      — The spinning search animation
  state_loot_preview.png   — The "Next" button or loot display on preview screen
  state_in_battle.png      — End battle button visible during a fight
  state_battle_result.png  — The star/loot result screen after battle

Capture them using:
  python tools/capture_template.py --name <name>
